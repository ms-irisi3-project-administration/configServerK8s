spring:
  datasource:
    url: jdbc:mysql://${DATABASE-SERVICE}/gestion_virement_ms?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC


spring:
  datasource:
    username: root
spring:
  datasource:
    password:

local:
  language: fr
local:
  country: FR
local:
  bundel: messages

eureka:
  instance:
    preferIpAddress: true
    hostname: eureka-0
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://eureka-lb/eureka
  zipkin:
    base-url: http://zipkin-svc/

management:
  endpoint:
    health:
      show-details: "ALWAYS"
  endpoints:
    web:
      exposure:
        include: "*"
